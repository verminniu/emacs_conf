;; 启动切换到svn代码目录下
;;(setq default-directory "e:/svn/")

;; set doxygen username
(setq user-full-name "verminniu")

;; 修改为左右打开窗口
(setq ediff-split-window-function (quote split-window-horizontally))
;;(setq-default ztree-dir-filter-list (cons "^.*\\.o" ztree-dir-filter-list))
(provide `my_config)
